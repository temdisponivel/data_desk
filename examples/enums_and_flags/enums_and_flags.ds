enum TestEnum
{
    ENUM_ONE,
    ENUM_TWO,
    ENUM_THREE,
    ENUM_MAX,
}

flags TestFlags
{
    FLAG_ONE,
    FLAG_TWO,
    FLAG_THREE,
}

// Enum without a name
enum
{
    NO_NAME_ENUM_ONE,
    NO_NAME_ENUM_TWO,
    NO_NAME_ENUM_THREE,
    NO_NAME_ENUM_MAX,
}

// Flags without a name
flags
{
    NO_NAME_FLAG_ONE,
    NO_NAME_FLAG_TWO,
    NO_NAME_FLAG_THREE,
}
